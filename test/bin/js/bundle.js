(function (factory) {
	typeof define === 'function' && define.amd ? define(factory) :
	factory();
}((function () { 'use strict';

	/**
	 * 作为运行文件时的入口文件
	 */
	long.Logger.isShowLog = true;
	long.Logger.log(null, "======>test long engine success.");

})));
